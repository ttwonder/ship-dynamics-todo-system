import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';import {execFileSync} from 'node:child_process';
const base='7bc51d3a1fdb416c1aa9d99d00fe6f1900c47afb';
const changes=[{"path": "src/types.ts", "before": "  snapshot?: MorningReportSnapshot;", "after": "  snapshot?: MorningReportSnapshot;\n  /** Read-only scoped projection, never a snapshot or writable business field. */\n  readonly __recordSnapshotAvailable?: true;"}, {"path": "src/normalize.ts", "before": "      snapshot: validSnapshot,", "after": "      snapshot: validSnapshot,\n      ...(!Object.prototype.hasOwnProperty.call(item,'snapshot') && item.__recordSnapshotAvailable === true ? {__recordSnapshotAvailable:true as const} : {}),"}, {"path": "src/morningHistory.ts", "before": "Boolean(report.businessDate) && Boolean(report.snapshot)", "after": "Boolean(report.businessDate) && (Boolean(report.snapshot) || report.__recordSnapshotAvailable === true)"}, {"path": "supabase/development/20260908_appdata_record_scoped_read.sql", "before": "  return result-'snapshot';", "after": "  result:=result-'__recordSnapshotAvailable';\n  if jsonb_typeof(p_body->'snapshot')='object' and jsonb_typeof(p_body->'snapshot'->'vessels')='array' and jsonb_typeof(p_body->'snapshot'->'tasks')='array' and jsonb_typeof(p_body->'snapshot'->'meetings')='array' then\n    result:=result || jsonb_build_object('__recordSnapshotAvailable',true);\n  end if;\n  return result-'snapshot';"}, {"path": "src/cloudRecordScopes.ts", "before": "    delete v.snapshot;", "after": "    const snapshot=v.snapshot;\n    if(object(snapshot)&&Array.isArray(snapshot.vessels)&&Array.isArray(snapshot.tasks)&&Array.isArray(snapshot.meetings))v.__recordSnapshotAvailable=true;\n    delete v.snapshot;"}, {"path": "src/cloudBlockPatch.ts", "before": "    if(value===null)continue;", "after": "    if(value===null)continue;\n    if(operation.collection==='agendaReports'&&Object.prototype.hasOwnProperty.call(value,'__recordSnapshotAvailable'))throw new TypeError('record-summary-not-writable');"}, {"path": "src/App.tsx", "before": "  const [reportPreviewOpen, setReportPreviewOpen] = useState(false);", "after": "  const reportActionGeneration=useRef(0);\n  const [reportPreviewOpen, setReportPreviewOpen] = useState(false);"}, {"path": "src/App.tsx", "before": "    invalidatePendingTaskOpen();\n    setSelectedVesselDetailId('');", "after": "    ++reportActionGeneration.current;\n    invalidatePendingTaskOpen();\n    setSelectedVesselDetailId('');"}, {"path": "src/App.tsx", "before": "const statsOwner=nextTab==='stats'?", "after": "const statsOwner=(nextTab==='stats'||nextTab==='reports')?"}, {"path": "src/App.tsx", "before": "['dashboard','total','closed','work','internalControl','meeting','stats'] as Tab[]", "after": "['dashboard','total','closed','work','internalControl','meeting','stats','reports'] as Tab[]"}, {"path": "src/App.tsx", "before": "  const openReportPreview = async () => {\n    if(!await loadRecordActionScope('full'))return;", "after": "  const captureReportAction=()=>{\n    const generation=++reportActionGeneration.current;\n    const actor=liveCurrentUserId.current,session=identitySessionGeneration.current,epoch=liveAuthorizationEpoch.current;\n    const config=getSupabaseConfig(),token=configIoCoordinator.current.begin(config);\n    return ()=>generation===reportActionGeneration.current&&actor===liveCurrentUserId.current&&session===identitySessionGeneration.current&&epoch===liveAuthorizationEpoch.current&&configIoCoordinator.current.isCurrent(token,getSupabaseConfig());\n  };\n  const closeReportPreview=()=>{\n    ++reportActionGeneration.current;\n    setReportPreviewOpen(false);setReportPreviewHistoryId('');setReportPreviewLiveItinerarySnapshot(null);\n  };\n  const openReportPreview = async () => {\n    const isCurrent=captureReportAction();\n    if(!await loadRecordActionScope('full',isCurrent)||!isCurrent())return;"}, {"path": "src/App.tsx", "before": "    setReportPreviewLiveItinerarySnapshot(snapshot);\n    setReportPreviewHistoryId('');", "after": "    if(!isCurrent())return;\n    setReportPreviewLiveItinerarySnapshot(snapshot);\n    setReportPreviewHistoryId('');"}, {"path": "src/App.tsx", "before": "catch(error){console.error('Itinerary report refresh failed',error);return alert('無法確認最新正式 Itinerary；本次未開啟正式報告，請稍後重試');}", "after": "catch(error){if(!isCurrent())return;console.error('Itinerary report refresh failed',error);return alert('無法確認最新正式 Itinerary；本次未開啟正式報告，請稍後重試');}"}, {"path": "src/App.tsx", "before": "  const openHistoricalReport=(report:AgendaReport)=>{\n    if(!canExportReports||report.kind!=='daily-morning'||!report.snapshot)return alert('此筆早會歷史目前沒有可檢視的快照。');", "after": "  const openHistoricalReport=async(summary:AgendaReport)=>{\n    const isCurrent=captureReportAction();\n    if(!canExportReports||summary.kind!=='daily-morning')return alert('此筆早會歷史目前沒有可檢視的快照。');\n    if(!await loadRecordActionScope({targets:[{collection:'agendaReports',id:summary.id}]},isCurrent)||!isCurrent())return;\n    const report=liveData.current.agendaReports.find(item=>item.id===summary.id);\n    if(!report||report.kind!=='daily-morning'||!report.snapshot)return alert('此筆早會歷史目前沒有可檢視的快照。');"}, {"path": "src/App.tsx", "before": "close={()=>{setReportPreviewOpen(false);setReportPreviewHistoryId('');setReportPreviewLiveItinerarySnapshot(null);}} onPrint={printReport}", "after": "close={closeReportPreview} onPrint={printReport}"}, {"path": "src/App.tsx", "before": "    const expectedAuthorizationEpoch=authorizationEpoch;\n    let itineraryProjectionSnapshot:ItineraryProjectionSnapshot;", "after": "    const expectedAuthorizationEpoch=authorizationEpoch;\n    const ownerIsCurrent=captureReportAction();\n    if(!await loadRecordActionScope('full',ownerIsCurrent)||!ownerIsCurrent())return false;\n    let itineraryProjectionSnapshot:ItineraryProjectionSnapshot;"}, {"path": "src/App.tsx", "before": "      console.error('Itinerary formal snapshot refresh failed',error);", "after": "      if(!ownerIsCurrent())return false;\n      console.error('Itinerary formal snapshot refresh failed',error);"}, {"path": "src/App.tsx", "before": "    const baseline=liveData.current;\n    const actor=baseline.users.find(user=>user.id===currentUser.id&&user.isActive);", "after": "    if(!ownerIsCurrent())return false;\n    const baseline=liveData.current;\n    const actor=baseline.users.find(user=>user.id===currentUser.id&&user.isActive);"}, {"path": "src/App.tsx", "before": "return Boolean(latest&&sameCloudConfig(cfg,latest)&&cloudIdentity(latest)===expectedIdentity&&liveCurrentUserId.current===requestUserId);", "after": "return Boolean(ownerIsCurrent()&&latest&&sameCloudConfig(cfg,latest)&&cloudIdentity(latest)===expectedIdentity&&liveCurrentUserId.current===requestUserId);"}, {"path": "src/App.tsx", "before": "const config=getSupabaseConfig(),token=configIoCoordinator.current.begin(config);", "after": "const config=getSupabaseConfig(),token=config?configIoCoordinator.current.begin(config):null;"}, {"path": "src/cloud.ts", "before": "  const cleanPayload = sanitizeAppDataForStorage(payload);", "after": "  if(payload.agendaReports.some(report=>Object.prototype.hasOwnProperty.call(report,'__recordSnapshotAvailable')))throw new CloudBlockPatchRejectedError('record-summary-not-writable');\n  const cleanPayload = sanitizeAppDataForStorage(payload);"}, {"path": "src/App.tsx", "before": "return ()=>generation===reportActionGeneration.current&&actor===liveCurrentUserId.current&&session===identitySessionGeneration.current&&epoch===liveAuthorizationEpoch.current&&configIoCoordinator.current.isCurrent(token,getSupabaseConfig());", "after": "return ()=>generation===reportActionGeneration.current&&actor===liveCurrentUserId.current&&session===identitySessionGeneration.current&&epoch===liveAuthorizationEpoch.current&&(token?configIoCoordinator.current.isCurrent(token,getSupabaseConfig()):!getSupabaseConfig());"}, {"path": "src/App.tsx", "before": "const loadRecordActionScope=async(scope:RecordReadScope,ownerIsCurrent:()=>boolean=()=>true):Promise<boolean>=>{", "after": "const loadRecordActionScope=async(scope:RecordReadScope,ownerIsCurrent:()=>boolean=()=>true,forceFresh=false):Promise<boolean>=>{"}, {"path": "src/App.tsx", "before": "if(config?.readMode!=='scoped-v1'||recordScopeKey(recordReadScope.current)===recordScopeKey(scope))return true;", "after": "if(config?.readMode!=='scoped-v1'||(!forceFresh&&recordScopeKey(recordReadScope.current)===recordScopeKey(scope)))return true;"}, {"path": "src/App.tsx", "before": "loadRecordActionScope({targets:[{collection:'agendaReports',id:summary.id}]},isCurrent)", "after": "loadRecordActionScope({targets:[{collection:'agendaReports',id:summary.id}]},isCurrent,true)"}, {"path": "src/App.tsx", "before": "    const allowed=new Set(activeVessels.map(vessel=>vessel.id));\n    setAgendaSelection(report.vesselIds.filter(id=>allowed.has(id)));", "after": "    const latest=liveData.current,actor=latest.users.find(user=>user.id===liveCurrentUserId.current&&user.isActive);\n    if(!actor)return;\n    const canViewAll=actor.role==='owner'||actor.role==='admin'||hasPermission(latest.settings.rolePermissions,actor,'viewAllVessels');\n    const allowed=new Set(latest.vessels.filter(vessel=>vessel.isActive&&vesselMatchesUser(vessel,actor,canViewAll)).map(vessel=>vessel.id));\n    if(!canViewAll&&(!report.vesselIds.length||!report.vesselIds.every(id=>allowed.has(id))))return alert('此筆早會歷史目前沒有可檢視的快照。');\n    setAgendaSelection(report.vesselIds.filter(id=>allowed.has(id)));"}, {"path": "src/normalize.ts", "before": "!Object.prototype.hasOwnProperty.call(item,'snapshot') && item.__recordSnapshotAvailable === true", "after": "item.snapshot === undefined && item.__recordSnapshotAvailable === true"}];
const old=p=>execFileSync('git',['show',base+':'+p],{encoding:'utf8'}).replaceAll('\r\n','\n'),now=p=>fs.readFileSync(p,'utf8').replaceAll('\r\n','\n');
const changed=new Set(changes.map(c=>c.path));for(const p of changed){let expected=old(p);for(const c of changes.filter(c=>c.path===p)){assert.equal(expected.split(c.before).length,2,p+' unique original span');expected=expected.replace(c.before,c.after);}assert.equal(now(p),expected,p+' exact authorized replacement set');}
const all=execFileSync('git',['ls-tree','-r','--name-only',base],{encoding:'utf8'}).trim().split('\n').filter(p=>p.startsWith('src/')||p.startsWith('supabase/')||['index.html','ship-itinerary.html','package.json','vite.config.ts'].includes(p));for(const p of all.filter(p=>!changed.has(p)))assert.equal(now(p),old(p),p+' frozen');
const result={status:'PASS',layer:'exact-outer-UI-byte-span-boundary',base,productPaths:[...changed].sort(),frozenFiles:all.length-changed.size,onlyJsxWiringDelta:'ReportPreviewModal.close -> closeReportPreview; original controls, copy, styles, layout, role predicates and component bodies unchanged'};console.log(JSON.stringify(result));if(process.env.QA_EVIDENCE_ROOT)fs.writeFileSync(path.join(process.env.QA_EVIDENCE_ROOT,'boundary.json'),JSON.stringify(result,null,2));
